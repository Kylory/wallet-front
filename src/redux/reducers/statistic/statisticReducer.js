const initialState = {
  statisticList: [],
  year: [],
  month:[],
};

export const phoneReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

